#include <stdio.h>
#include <math.h>
long int fibo(long int prenum,long int num);
int main(int argc, char const *argv[])
{
    long int a = 2, d = 1, sum = 0, count = 1, esum = 0;

    printf("%d", esum);
    return 0;
}
long int fibo(long int prenum,long int num)
{
     
    
}
