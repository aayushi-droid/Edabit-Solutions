#include<iostream>
using namespace std;
int main() {
	int n;
	cin>>n;  //input number
	int d; 
  cin>>d; //digit to be counted
	int count=0;
	while(n>0)
	{
		int r;
		r=n%10;
		if(r==d)
		{
			count++;
		}
		else
		{
			break;
		}
		n=n/10;
	}
	cout<<count;
	return 0;
}
